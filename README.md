# JavaScript Libraries 

When developing a project with framework-less approach, you need a set of building blocks, which ideally are small and easy to replace.

This repository contains several small libraries (currently only one, but there will be more) that can be used to develop front-end functionality, primarily single-page applications.